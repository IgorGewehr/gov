namespace Tensorroot.Gov.Modules.Transparencia.Domain;

/// <summary>
/// Módulo Transparencia — Camada de Domínio (agregados, entidades ricas, Value Objects, eventos de domínio, invariantes).
/// Marca de montagem (assembly marker) para varredura por reflexão (MediatR /
/// FluentValidation / Injeção de Dependência) e para os testes de arquitetura.
/// Não possui comportamento.
/// </summary>
public sealed class AssemblyReference;
